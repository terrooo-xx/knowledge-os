---
type: task-log
status: draft
domain: 系统治理
created: 2026-08-12
updated: 2026-08-12
---

# 阶段⑬：架构去重与结构治理 —— 任务报告

> 目标：在现有架构已完成真实验证的基础上，清理重复组件、归档废弃文档、统一目录职责、修正引用，保持现有工作流与 MCP 查询能力完全不受影响。
> 原则：不重新设计架构、不删除历史资产（归档优先）、不覆盖用户未提交修改、不自动 commit/push。

## 一、本次实际修改

| 文件/目录 | 原位置 | 新位置 | 原因 |
|---|---|---|---|
| 4 个 Agent 工作流文档 | `.agents/agents/` | `90_System/archive/agents/` | 早期简化版，已被 `.agents/skills/` 取代 |
| 11 个阶段报告 | `90_System/阶段XX_*.md` | `90_System/archive/stages/` | 历史建设记录，不与活动系统文件混放 |
| MCP 查询接口（6 文件） | `90_System/agent/` | `90_System/rag/interface/` | 消除 agent/agents/skills 概念混淆，归入 RAG 职责 |
| `50_Reviews/` 三个子目录 | `50_Reviews/每周复盘|知识缺口|过期内容检查` | `40_Outputs/reviews/` | 合并输出层，职责交叉清理 |
| RAG 回答提示词 | `90_System/prompts/rag_answer.md` | `90_System/rag/prompts/rag_answer.md` | 按功能归属：RAG 提示词归 RAG |
| 空目录 + 占位文件 | `90_System/logs/`、`90_System/schemas/`、`interfaces.md` | 删除 | 无引用、无实际功能的占位 |

### 修改的引用与配置

- `KNOWLEDGE_OS.md`：目录职责表、重要文件表、权限模型、目录树、Knowledge Gap 区分、archive 定位。
- `90_System/rag/interface/` 内部路径反推：`knowledge_service.py`（`RAG_DIR=AGENT_DIR.parent`，VAULT 反推改为 3 层）、`mcp_server.py`（config.yaml 路径 `parent.parent/config.yaml`、VAULT 反推 4 层）、`benchmark_query.py`（`RAG_DIR=AGENT_DIR.parent`、VAULT 反推 3 层）、`knowledge_cli.py` docstring、`README.md`（含修复既有 `\a` 控制字符转义 bug）。
- `~/.codex/config.toml`：`[mcp_servers.knowledge-os]` args 路径改为 `90_System\rag\interface\mcp_server.py`（需重启 Codex 生效）。
- 测试：`test_agent_knowledge_service.py`、`test_mcp_server.py` 的 AGENT_DIR 路径。
- `llm/prompt.py` DEFAULT_TEMPLATE、`config.yaml` / `tests/config.local.yaml` 的 template 路径 → `90_System/rag/prompts/rag_answer.md`。
- `knowledge_os_check.ps1`：Expected 列表同步（移除 50_Reviews/logs/schemas/agents，新增 40_Outputs/reviews、rag/interface、rag/prompts、archive 子目录），删除 interfaces.md 空占位检查。
- 文档引用：根 `AGENTS.md`、`README.md`、`HOME.md`、`rag/README.md`、`rag/AGENTS.md`、`.agents/skills/weekly-review/SKILL.md`、`90_System/prompts/每周知识库复盘.md`。

## 二、RAG 脚本职责（最终）

- `update_index.py` = `main_vector_db` 的标准索引入口（20_Wiki + 30_Projects，全量 / `--changed` 增量 / `--file` 单文档）。
- `ingest_rag.py` = raw / 特殊 ingestion（`--target raw` → raw_vector_db；`--target wiki` → wiki_vector_db）；`--target main` 已移除，`default=raw`，测试同步更新。

## 三、归档内容

- `90_System/archive/agents/`：ingest_agent / retrieval_agent / review_agent / wiki_compile_agent（4 个，仅历史追溯）。
- `90_System/archive/stages/`：阶段06 / 07 / 08 / 09 / 10 / 10.5 / 11A / 11B / 12A / 12B / 12C（11 个阶段报告，仅历史追溯）。

## 四、删除内容

- `90_System/logs/.gitkeep`（空目录，日志职责由 `90_System/任务记录/` 与 `control_center/activity_log.jsonl` 承担）
- `90_System/schemas/.gitkeep`（空目录，无引用）
- `interfaces.md`（0 字节，无引用）

## 五、最终目录结构（关键）

```text
.agents/skills/                 # 当前工作流规范（knowledge-compiler / project-doc-maintainer / weekly-review）
40_Outputs/
├── 学习总结/ 技术方案/ 项目报告/ 对外材料/
└── reviews/                    # 每周复盘/ 知识缺口/ 过期内容检查（人工知识缺口）
90_System/
├── archive/
│   ├── agents/                 # 早期 Agent 文档（历史追溯）
│   └── stages/                 # 阶段06~12C 报告（历史追溯）
├── KNOWLEDGE_OS.md
├── control_center/
├── prompts/                    # 任务类提示词
└── rag/
    ├── interface/              # Agent 只读查询接口 + MCP Server
    ├── prompts/                # RAG 提示词（rag_answer / relevance_judge）
    ├── scripts/ rag_engine/ llm/ tests/ database/ cache/
```

## 六、验证结果

| 项目 | 结果 |
|---|---|
| Git 状态 | PASS：重命名/删除记录正确，无误删核心文件；用户未提交修改（update_changelog.ps1 等）保留未覆盖 |
| Python compile | PASS：全部关键 .py 通过 py_compile |
| 项目测试 | PASS：test_cli_contract / test_paths / test_agent_knowledge_service / test_mcp_server / test_control_center / test_rag_health / test_wiki_health / test_evidence_gaps / test_review / test_incremental_rag / test_main_query / test_full_chain / test_wiki_compiler / test_inbox_processor 全部通过 |
| RAG 查询 | PASS：`hybrid_query.py` 正常检索 `main_vector_db`（STM32 DMA Wiki + 移动底盘项目文档） |
| MCP 跨项目 | PASS：从 `C:\Temp\DroneTest`（cwd ≠ Knowledge OS）启动 `mcp_server.py`，initialize / tools/list / knowledge_search 正常，检索到 `20_Wiki/03_STM32/STM32-DMA-配置与使用.md`（fail-closed 为沙箱无 LLM 网络的预期行为） |
| Control Center | PASS：启动正常，页面 HTTP 200，health API 返回 rag/wiki/architecture 全部 ok，Activity Log 不报错 |
| knowledge_os_check.ps1 | PASS=95  WARNING=2  ERROR=0（2 个 WARNING 均为既有：00_Inbox 35 个待处理文件、Git 50 处未提交变更） |

## 七、未处理 / 待人工确认

1. **`90_System/scripts/update_changelog.ps1`（用户未提交修改）**：其中 `$prefixes` 仍含 `'50_Reviews/'` 和 `'interfaces.md'`。这两个前缀引用的目录/文件已删除，但属用户正在进行的工作，未改动；CHANGELOG 聚合逻辑不会因此报错，建议用户后续顺手清理。
2. **`CHANGELOG.md`**：历史记录未改写（保留旧的 50_Reviews/agent/ 路径引用，符合"不修改历史"原则）。
3. **`archive/stages/阶段12C`** 与 `archive/agents/*` 内含旧路径 `90_System/agent/` 的历史描述，按任务要求不修改历史文件内容。
4. **MCP 配置生效**：`~/.codex/config.toml` 已更新为 `90_System/rag/interface/mcp_server.py`，需**重启 Codex** 后新路径才加载。
5. **`90_System/rag/interface/__pycache__`**：git mv 随目录移动的本地缓存，已清理新位置缓存；属可删除的 L5 数据。
6. **Ingest 覆盖检查（任务 11）**：`config.yaml` `paths.inbox = "00_Inbox"`，`ingest_rag.py --target raw` 会 rglob 覆盖全部 6 个子目录；未发现重复规则或无效路径，00_Inbox 结构保持不动。

## 八、结论

成功标准达成：已归档确认的重复/废弃结构（agents 文档、阶段报告），澄清 RAG 脚本职责，统一输出与 Prompt 归属，清理空占位，同步所有引用与 KNOWLEDGE_OS.md；现有工作流、RAG 查询、MCP 跨项目接口与 Control Center 全部验证可用。未 commit / 未 push。